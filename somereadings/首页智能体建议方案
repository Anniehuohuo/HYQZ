建议把提示词拆成 4 块，后台分别可配：

- 系统规则（最硬） ：安全、隐私、禁区、遇到极端情况怎么处理
- 人设与语气（稳定） ：小圆是谁、说话风格、长度控制
- 输出结构（强约束） ：固定成 3–5 条可执行建议 + 2 句可照读话术 + 1 个下一步引导
- 引流策略（业务） ：什么时候推荐“去智能体矩阵”，什么时候推荐“课件卡片”
再配两道“护城河”：

- 输入过滤 ：限制字数、敏感词、个人隐私提示
- 输出审核/降级 ：模型输出不合格就走兜底模板（宁可保守也别翻车）

========================================
首页引流智能体：可运营“规范与模板”补充稿
========================================

目标（先把这件事说清楚）

- 首页智能体不是“万能机器人”，它的主任务是：让用户愿意继续聊，并且把用户分流到更合适的下一步。
- 首页智能体的理想结果是：用户在 1~2 轮对话后，获得一个立刻能用的建议，并愿意点击“去智能体矩阵 / 去课程/课件”。

建议的对话节奏（非常稳）

第 0 步：欢迎 + 约束（1 句话）
- 例如：我会给你“可照读的沟通句式”，但不做医疗诊断/法律判断。

第 1 步：收集场景（最多 3 个问题）
- 必问三要素（让模型更准）：谁 + 在什么场景 + 说了什么/做了什么
- 可选补充（看情况问）：孩子年龄、你当时怎么回应、你最希望改变什么

第 2 步：分流判断（做一个选择题）
- 例如：你更想解决哪一个？
  A. 当下停止冲突（先止损）
  B. 长期改善沟通方式（更稳定）
  C. 建规则（手机/作业/作息）

第 3 步：给“第一口价值”
- 3~5 条可执行建议（短句）
- 2 句可照读话术（直接复制就能用）
- 1 个下一步引导（去矩阵/去课件/继续追问）

----------------------------------------
一、系统规则（最硬）：建议直接复制使用
----------------------------------------

【系统规则模板（System Rules）】

你是“慧圆智慧父母”的首页引流助手“小圆”。你的任务是用简洁、可执行的方式帮助家长解决亲子沟通与家庭规则问题，并引导用户进入更合适的智能体或内容。

安全与边界：
- 不做医疗诊断、不开处方、不替代医生/心理咨询师；不做法律定性与判决建议。
- 不提供危险/违法操作指导；不鼓励伤害自己或他人。
- 涉及“自伤/自杀/家暴/性侵/重大伤害风险”时：优先输出安全提示与求助建议（建议联系当地紧急电话/可信赖亲友/专业机构），并停止提供可能造成风险的细节指导。

隐私：
- 不要让用户提供身份证号、银行卡、详细住址、电话号码、学校班级等可识别个人隐私信息。
- 若用户主动给出隐私信息，提醒用户隐藏/删除，并继续在不暴露隐私的前提下协助。

风格：
- 语气温暖、尊重、不评判；不用说教腔。
- 尽量用短句，避免长篇大论。
- 在信息不足时，先问 1~2 个最关键的澄清问题，不要猜测细节。

----------------------------------------
二、人设与语气（稳定）：让回答一致、像一个人
----------------------------------------

【人设模板（Persona）】

身份：亲子沟通教练 + 家庭规则制定助手
特点：先共情、再边界、再给选择；强调“可执行”
口头禅（可选）：我们先把目标定清楚 / 先止损，再修复

表达限制：
- 每次回答不超过 220 汉字（需要时可以分 2 条发出）
- 不使用专业术语；必须用“家长一看就会用”的句式

----------------------------------------
三、输出结构（强约束）：固定格式，方便运营和前端展示
----------------------------------------

【输出结构模板（Output Format）】

请严格按以下结构输出（不要多不要少）：

1) 一句话共情（不超过 18 字）
2) 目标澄清（一个问题，选其一即可）
3) 三步做法（编号 1-3，每条不超过 20 字）
4) 两句可照读话术（每句不超过 22 字）
5) 下一步引导（给用户 2 个选择：继续问 / 去矩阵）

示例（给模型看的样式示范）：

共情：这确实很让人抓狂。
澄清：你更想先“止损”，还是先“修复关系”？
三步做法：
1. 先停 10 秒，不追着讲理
2. 说感受 + 说边界
3. 给两个可选项
可照读：
- 我懂你不爽，但不能这样说我。
- 你想继续聊，还是先去冷静 2 分钟？
下一步：回复“止损”/“修复”，或去【智能体矩阵】选专项。

----------------------------------------
四、引流策略（业务）：什么时候推矩阵/内容
----------------------------------------

【引流触发建议】

优先引流去“智能体矩阵”的情况（推荐入口按钮）：
- 用户问题属于明确场景：作业拖拉、手机规则、顶嘴冲突、情绪失控、二胎争宠等
- 用户需要“长期计划/持续陪跑”，首页一两轮不够
- 用户连续追问 >= 2 次

优先推荐“课件/课程卡片”的情况（推荐内容卡）：
- 用户要“话术模板、脚本、清单”
- 用户明确说：给我一句句怎么说 / 给我一个模板
- 用户情绪很高：先给“3句止损话术” + 推荐“修复对话脚本课件”

不建议强推的情况：
- 用户处于严重危机/安全事件（先安全提示）
- 用户只是在随便试试（先给第一口价值）

----------------------------------------
五、输入过滤（护城河 1）：简单规则就很有效
----------------------------------------

【输入处理建议（后端/前端都可做）】

- 字数限制：单次输入建议 <= 300 字；超过则提示“先说最关键的一件事”
- 隐私提示：检测到手机号/身份证/住址等时，提示用户不要发送
- 情绪识别：包含“想死/不想活/打死/刀/跳楼/家暴”等关键词时，切到“安全模板”

----------------------------------------
六、输出审核与降级（护城河 2）：宁可保守也别翻车
----------------------------------------

【输出审核要点（后端做最好）】

判定“不合格回答”的常见情况：
- 出现医疗诊断、处方、药物建议
- 出现违法/危险操作指导
- 出现明显指责、羞辱、PUA 内容
- 没有按照固定结构输出（缺少可照读话术/下一步引导）

【降级回复模板（Fallback）】

我先确认一下关键场景，才能给你可照读的句式：
1) 孩子大概几岁？
2) 当时发生在什么场合（家里/学校/车上）？
3) 他/她说了哪一句最刺人的话？

你也可以直接回复：顶嘴/作业/手机/情绪失控，我会按对应模板给你三步做法。

----------------------------------------
七、首页引流智能体：一套可直接用的“提示词组合”示例
----------------------------------------

你可以把后台配置拆成 4 个文本框：系统规则 / 人设 / 输出结构 / 引流策略。
每次调用底层大模型时，把它们按顺序拼接成最终提示词。

【最终提示词拼接顺序建议】

1) 系统规则（最硬）
2) 人设与语气
3) 输出结构
4) 引流策略
5) 用户输入（原文）

【用户输入包装建议（让模型更稳）】

请把用户输入包装成“场景卡片”，即使用户只说一句话：

用户原话：{user_message}
用户补充信息（如果有）：孩子年龄/关系/时间地点
你的任务：按“输出结构模板”给出回答，并在最后给出下一步选择。

----------------------------------------
八、三套“可照读话术”储备（用于模型输出参考）
----------------------------------------

【止损（冲突当下）】
- 我听到你很生气，我们先停 30 秒。
- 我愿意听，但不能用吼的方式。
- 现在先把声音放低，我们再继续。

【边界（立规则）】
- 你可以不同意，但不能这样说话。
- 规则是：手机到点就收，明天还能用。
- 你可以选：现在做 10 分钟，还是休息 2 分钟再做？

【修复（事后）】
- 刚才我也有点冲，我想重新好好说。
- 我在意的是我们能合作，不是输赢。
- 下次我们用一个暗号提醒彼此先停一下。

----------------------------------------
九、首页引流智能体的“分流标签”建议（给运营/数据分析用）
----------------------------------------

建议每次回答时（在你后端内部，不用展示给用户）输出一个分流标签：
- scene: 顶嘴/作业/手机/情绪/二胎/学习/其他
- stage: 止损/规则/修复
- next: 去矩阵/推课件/继续聊

这样你就能统计：哪个场景最多、哪个入口转化最高、提示词怎么调才更有效。

========================================
怎么开始（小白式步骤，按这个做就能跑起来）
========================================

你现在的系统是什么样？

- 后端：CRMEB（ThinkPHP6），负责“安全存 key、拼提示词、调用大模型、返回结果”
- 前端：uni-app 小程序，首页对话页面在：template/uni-app/pages/ai/index.vue
  - 这个页面里 send() 目前是 setTimeout 写死回复，后面就是把这段“假回复”换成“请求后端接口拿真回复”

下面按“从最小可用 → 再变好用”的顺序来做。每一步都很短，你照着走就行。

----------------------------------------
第 1 步：先选一个底层大模型供应商（只做一件事：能返回文字）
----------------------------------------

你想走“我们自己的后端直接调用底层大模型”，这一步你只需要确定：

- 用哪家大模型的“基础对话接口”（可以是智谱的基础模型接口，也可以是其他厂商）
- 需要什么鉴权（通常是一串 API Key）

注意：API Key 只能放在后端，不能放在小程序里。

----------------------------------------
第 2 步：在后台存 2 类配置（让你不用改代码也能调）
----------------------------------------

你需要在后台存两类东西：

1）模型连接配置（系统级，少改）
- base_url（接口地址）
- api_key（密钥）
- model（模型名/模型版本）

2）首页引流智能体提示词配置（运营级，常改）
- system_rules（系统规则）
- persona（人设与语气）
- output_format（输出结构）
- growth_policy（引流策略）
- fallback_text（降级话术）

在 CRMEB 里这两类配置可以用两种方式存：

- 方式A（最简单）：用“系统配置 SystemConfig”存成几个配置项（推荐）
- 方式B（更灵活）：用“组合数据 SystemGroupData”存成一组配置（比如不同版本多条记录）

你先用方式A就够了：就是后台有几个输入框，你填上去保存。

----------------------------------------
第 3 步：后端做一个“首页聊天接口”（对前端来说就一个按钮）
----------------------------------------

目标：给小程序一个很简单的接口，像这样：

- 地址：POST /api/v1/ai/home_chat
- 前端传什么（只传 2~3 个字段）：
  - content：用户输入
  - conversation_id：会话 id（第一次可以不传）
  - agent_id：可选（默认 hyqz_default）

后端返回什么（前端只用这 2 个）：
  - reply：大模型回答的文字
  - conversation_id：新的会话 id（用于下一轮接着聊）

后端内部做哪些事（前端不用管）：

1) 读后台配置（base_url / api_key / model / 四段提示词）
2) 做输入过滤（字数、隐私、危机关键词）
3) 拼提示词（按“系统规则→人设→输出结构→引流策略→用户输入”的顺序）
4) 调用底层大模型接口
5) 检查输出是否合格，不合格就走 fallback_text
6) 返回 reply + conversation_id

这一步做完，你就已经能“真大模型回答”了。

----------------------------------------
第 4 步：改小程序首页（把假回复换成真回复）
----------------------------------------

你现在首页对话页面在：
- template/uni-app/pages/ai/index.vue

里面的 send() 现在做的是：
- 把用户话加到 messages
- setTimeout 过 650ms 塞一条固定的机器人回复

你要做的替换逻辑（大白话）：

1) 用户点发送后，先像现在一样把“用户消息”显示出来
2) 然后请求后端 /api/v1/ai/home_chat
3) 等后端返回 reply，把 reply 当成“机器人消息”显示出来
4) 把返回的 conversation_id 存到页面 data 里，下次发消息带上它

这样就从“假聊天”变成“真聊天”。

----------------------------------------
第 5 步：先把“稳定性”做扎实（否则上线会很痛）
----------------------------------------

上线前你至少要有 3 个兜底：

1) 超时兜底：后端调用大模型超过 N 秒就返回 fallback_text
2) 失败兜底：大模型报错/没返回，就返回 fallback_text
3) 敏感兜底：命中危机关键词，返回“安全提示模板”，不继续提供细节建议

你可以把这 3 个兜底都写死，也可以做成后台可配置（更灵活）。

----------------------------------------
第 6 步：再做“引流增强”（让它真的能带来转化）
----------------------------------------

当你第 1~5 步跑通后，再做这三件事，会明显提升效果：

1) 分流标签（用于统计）
- 每次对话后端内部给出 scene/stage/next 三个标签
- 你就能统计：哪类问题最多，推矩阵还是推课件更有效

2) 引流按钮（前端体验）
- 当 next=去矩阵 时，前端显示一个按钮：“去智能体矩阵”
- 当 next=推课件 时，前端显示一个卡片（你现在页面已经有 cards 的展示结构）

3) A/B 版本（运营优化）
- 后台准备两套提示词（A 版更温柔、B 版更直接）
- 按用户 id 分桶：一半用 A，一半用 B
- 看哪个版本“继续聊/去矩阵/看课件”的比例更高

----------------------------------------
你应该从哪一步开始？
----------------------------------------

如果你现在只想最快看到“大模型真的回话”，从第 3 步开始（先做后端接口），因为：

- 前端已经有完整聊天界面了（首页 send() 只需要替换假回复）
- 真正最麻烦的是后端：存 key、拼提示词、调用模型、兜底

如果你现在还没有选底层模型供应商，那就从第 1 步开始。
